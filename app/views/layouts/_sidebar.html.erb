<br/><br/>

  <div  id='sidebar' class="nav flex-column nav-pills" id="v-pills-tab" role="tablist" aria-orientation="vertical" >
      <a class="nav-link active" id="v-pills-home-tab"  href="<%= "#{articles_path}" %>" role="tab" aria-controls="v-pills-home" aria-selected="true">All Articles</a>
      <% if user_signed_in? %>
      <a class="nav-link" id="v-pills-profile-tab"  href="<%= "#{new_article_path}" %>"  role="tab" aria-controls="v-pills-profile" aria-selected="true">Add New Article</a>
     <% end %>

    
    </div>