<nav class="navbar navbar-expand-md fixed-top navbar-light " 
 style="background-color: #d2cdd1;border-bottom: 1px solid white">
  <a class="navbar-brand" href="<%= "#{root_path}" %>">Blog & ROR</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent" >
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="<%= "#{root_path}" %>">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= "#{root_path}" %>" title='to be continued...'>About</a>
      </li>
      
      <li class="nav-item">
        <a class="nav-link " href="<%= "#{root_path}" %>" title='to be continued...'>Terms</a>
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
  <div class="logitem" >
   <% if user_signed_in? %>
   <%=  link_to current_user.username,edit_user_registration_path %> &nbsp;&nbsp;|
        <%= link_to 'Sign Out',destroy_user_session_path,method: :delete, class: 'btn btn-lighter' %> 

<% else %>

	<%= link_to 'Sign In',new_user_session_path, class: 'btn btn-lighter' %> | 
    <%= link_to 'Sign Up',new_user_registration_path, class: 'btn btn-lighter' %> 
    <% end %>
</div>
    </form>
  </div>
